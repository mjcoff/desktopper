# desktopper
Simple GUI interface for creating .desktop files for GNOME.

Enter a display name, the path to the file you want to execute, and the path to the icon to display. desktopper then creates a .desktop file (default location is ~/.local/share/applications) so that you can easily launch the application from the GNOME application picker.
